proxyquire = require('proxyquire')
chai = require('chai')
expect = chai.expect
should = chai.should()
assert = chai.assert

describe 'actions', ->
  describe 'login', ->
   it 'should assume auth method if only one is required', (done) ->
     done()
